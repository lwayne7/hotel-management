# Contract Artifacts

- `openapi-snippet.json`：从当前后端接口中抽出的 OpenAPI 片段，作为面试演示时的契约样例。
- 用途：说明项目不仅有 Swagger 在线文档，也保留了可脱离运行环境展示的接口契约材料。

## 建议讲法

- 这是“契约样例”而不是完整导出文件，重点是展示接口字段、分页结构和响应约束。
- 如果面试官追问完整契约管理，可以继续展开到 Swagger 自动生成、前后端共享 DTO、CI 中做 contract diff 等方向。
